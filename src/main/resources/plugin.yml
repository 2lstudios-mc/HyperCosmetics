name: ${name}
description: ${description}
author: ${author}
main: ${mainClass}
version: ${version}
url: ${url}
commands:
    hypercosmetics:
        description: "Main command for cosmetics"
        aliases: [ "cosmetics" ]